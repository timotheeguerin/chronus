---
# Change versionKind to one of: breaking, feature, fix, internal
changeKind: feature
packages:
  - "@chronus/chronus"
---

Replace `pacote` dependency with a lightweight `fetch`-based registry helper and `nanotar` for reading tarball manifests
