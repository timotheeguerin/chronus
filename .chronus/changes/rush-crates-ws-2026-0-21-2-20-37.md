---
# Change versionKind to one of: breaking, feature, fix, internal
changeKind: feature
packages:
  - "@chronus/chronus"
---

Add support for non node monorepo. Updated workspace type to have category prefix(e.g. `npm` -> `node:npm`) Old name kept as alias for backward compatibility
